EMAIL_FROM=iConstruct <onboarding@resend.dev>

# Required for iConstruct AI (Gemini via Google AI Studio — no custom training)
# firebase functions:secrets:set GEMINI_API_KEY
GEMINI_API_KEY=

# Optional fallback if Gemini fails
# OPENAI_API_KEY=
